Sosu
Sosu は、Rust で開発された効率的な素数生成プログラムです。このプロジェクトはセグメント法を使用して大規模な素数を生成し、並列処理を利用して高速化を実現しています。

特徴
セグメント法を使用: 範囲を分割して効率的に素数を計算。
並列処理: rayon クレートを利用して大規模な計算を高速化。
設定可能: 設定ファイル（settings.txt）で生成の詳細を調整可能。
出力: 計算された素数を primes.txt に保存。

必要条件
このプロジェクトを実行するには、以下の環境が必要です：
Rust 1.65.0 以上
Cargo（Rust に含まれるビルドツール）

クレート依存
このプロジェクトでは以下のクレートを使用しています：
rayon: 並列処理のためのクレート。
bitvec: 素数計算の効率的なビット操作を実現。
serde: 設定ファイルの読み書き。
toml: TOML フォーマットの設定ファイルを扱うためのクレート。

インストールと実行
以下の手順でプロジェクトを実行できます：

リポジトリをクローン:
git clone https://github.com/riragon/sosu-seisei.git
cd sosu-seisei
cargo run --release

設定ファイル (settings.txt)
このプログラムは、設定ファイル (settings.txt) を使用して動作を制御します。以下は、設定可能なオプションです：

prime_cache_size (デフォルト: 100,000): 初期の素数のキャッシュサイズ。
segment_size (デフォルト: 10,000,000): 各セグメントの範囲。
chunk_size (デフォルト: 16,384): 並列処理に使用するチャンクのサイズ。
writer_buffer_size (デフォルト: 8MB): 書き込みバッファのサイズ。
prime_max (デフォルト: 10,000,000,000): 素数を計算する最大範囲。

出力ファイル (primes.txt)
計算されたすべての素数が、このファイルに行ごとに出力されます。
ファイルはプログラムの実行ごとに上書きされます。

プロジェクト構成
sosu-seisei/
├── src/
│   └── main.rs          # メインプログラム
├── settings.txt         # プログラムの設定ファイル
├── primes.txt           # 計算結果の出力ファイル
├── Cargo.toml           # プロジェクト設定
├── Cargo.lock           # 依存関係のロックファイル
└── .gitignore           # Gitで無視するファイルやディレクトリ


以下は、README.md に記載すべき内容の具体例です。この内容は、提供されたコードとプロジェクトの構造に基づいて作成されています。

Sosu
Sosu は、Rust で開発された効率的な素数生成プログラムです。このプロジェクトはセグメント法を使用して大規模な素数を生成し、並列処理を利用して高速化を実現しています。

特徴
セグメント法を使用: 範囲を分割して効率的に素数を計算。
並列処理: rayon クレートを利用して大規模な計算を高速化。
設定可能: 設定ファイル（settings.txt）で生成の詳細を調整可能。
出力: 計算された素数を primes.txt に保存。
必要条件
このプロジェクトを実行するには、以下の環境が必要です：

Rust 1.65.0 以上
Cargo（Rust に含まれるビルドツール）
クレート依存
このプロジェクトでは以下のクレートを使用しています：

rayon: 並列処理のためのクレート。
bitvec: 素数計算の効率的なビット操作を実現。
serde: 設定ファイルの読み書き。
toml: TOML フォーマットの設定ファイルを扱うためのクレート。
インストールと実行
以下の手順でプロジェクトを実行できます：

リポジトリをクローン:

bash
コードをコピーする
git clone https://github.com/riragon/sosu-seisei.git
cd sosu-seisei
リリースビルドで実行:

bash
コードをコピーする
cargo run --release
結果の確認:

計算された素数は primes.txt に出力されます。
設定ファイル (settings.txt)
このプログラムは、設定ファイル (settings.txt) を使用して動作を制御します。以下は、設定可能なオプションです：

prime_cache_size (デフォルト: 100,000): 初期の素数のキャッシュサイズ。
segment_size (デフォルト: 10,000,000): 各セグメントの範囲。
chunk_size (デフォルト: 16,384): 並列処理に使用するチャンクのサイズ。
writer_buffer_size (デフォルト: 8MB): 書き込みバッファのサイズ。
prime_max (デフォルト: 10,000,000,000): 素数を計算する最大範囲。
出力ファイル (primes.txt)
計算されたすべての素数が、このファイルに行ごとに出力されます。
ファイルはプログラムの実行ごとに上書きされます。
プロジェクト構成
plaintext
コードをコピーする
sosu-seisei/
├── src/
│   └── main.rs          # メインプログラム
├── settings.txt         # プログラムの設定ファイル
├── primes.txt           # 計算結果の出力ファイル
├── Cargo.toml           # プロジェクト設定
├── Cargo.lock           # 依存関係のロックファイル
└── .gitignore           # Gitで無視するファイルやディレクトリ


使用方法
プログラムを実行すると、設定ファイル (settings.txt) が自動生成されます（存在しない場合）。
必要に応じて settings.txt を編集して設定を変更します。
再度プログラムを実行すると、指定された設定で素数を計算します。

大規模な素数生成を行う場合、計算時間やディスク容量に注意してください。
出力される primes.txt のサイズが非常に大きくなる可能性があります。

ライセンス
このプロジェクトはMITライセンスのもとで公開されています。詳細は LICENSE ファイルを参照してください。

